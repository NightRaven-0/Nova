# Core — everything needed to run the default fully-offline Nova (nova.py).
python-dotenv>=1.0.1
sounddevice>=0.4.7
numpy>=1.26.0
openai>=1.40.0          # client for both OpenAI and Ollama's OpenAI-compatible API
faster-whisper>=1.0.0   # offline GPU speech-to-text
piper-tts>=1.2.0        # offline text-to-speech

# Optional backends:        pip install -r requirements-optional.txt
# Phase-1 VAE / training:    pip install -r requirements-vae.txt
